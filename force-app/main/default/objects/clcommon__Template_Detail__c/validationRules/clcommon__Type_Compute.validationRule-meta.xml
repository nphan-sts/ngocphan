<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>clcommon__Type_Compute</fullName>
    <active>true</active>
    <description>Record with Type = COMPUTE must define an expression.</description>
    <errorConditionFormula>AND(
    ISPICKVAL(clcommon__Type__c,&apos;COMPUTE&apos;),
    OR(
       ISBLANK(clcommon__Expression__c),
       NOT(ISBLANK(clcommon__Chart_of_Account__c)),
       NOT(ISBLANK(clcommon__Detail__c))
    )   
)</errorConditionFormula>
    <errorMessage>Record with type = COMPUTE must have an expression. Chart of Account or Detail must be blank</errorMessage>
</ValidationRule>
