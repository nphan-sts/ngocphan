<apex:page lightningStylesheets="{!$User.UIThemeDisplayed == 'Theme4d'}" standardController="genesis__Applications__c" extensions="genesis.InvoiceFactoringPricingCtrl" action="{!onLoad}">
<!-- <apex:outputPanel rendered="{!NOT(manager.isSF1Request)}">
     <apex:composition template="wizardComposition">
         <apex:define name="pageBody" >
            <apex:panelGrid columnClasses="col1,col2" columns="2" width="100%"  cellpadding="10" cellspacing="10" border="2" rules="all">
                 <apex:outputPanel layout="block" styleClass="outPanelLeft" >
                    <div class="button bordered back">
                        <span class="pointer"></span>
                        <div class="content">
                            <span class="label">
                                <apex:commandLink action="{!backActionLogic}" value="{!manager.backButtonText}" />
                            </span>
                        </div>
                    </div>
                </apex:outputPanel>
        
                <apex:outputPanel layout="block">
                    <div class="button bordered next">
                        <span class="pointer"></span>
                        <div class="content">
                            <span class="label">
                                <apex:commandLink action="{!nextActionLogic}" value="{!manager.nextButtonText}"/>
                            </span>
                        </div>
                    </div>
                </apex:outputPanel>
            
            </apex:panelGrid>
            <apex:pageBlock id="creditDecisionPBId">
                <apex:pageMessages />
                <apex:pageblockButtons >
                    <apex:commandButton value="Save" action="{!updateApplication}"/>
                </apex:pageblockButtons>
                <apex:outputpanel id="headerid">
                    <apex:pageBlockSection columns="2" id="creditPanelPageBlockSection">
                        <apex:outputField value="{!application.Credit_Limit__c}"/>
                        <apex:outputField value="{!application.Draw_Term__c}"/>
                        <apex:inputField value="{!application.Factor_Fee_Rate__c}"/>
                        <apex:inputField value="{!application.Holdback_Percent__c}"/>
                        <apex:inputField value="{!application.Collect_Factor_Fee__c}"/>
                        <apex:inputField value="{!application.Recourse__c}"/> 
                        <apex:inputField value="{!application.Late_Charge_Percent__c}"/> 
                        <apex:inputField value="{!application.Credit_Review__c}"/>                       
                    </apex:pageBlockSection>                  
                </apex:outputpanel>                       
            </apex:pageBlock>
        </apex:define> 
  </apex:composition>
</apex:outputPanel> -->
</apex:page>