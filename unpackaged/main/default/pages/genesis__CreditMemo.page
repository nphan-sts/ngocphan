<apex:page lightningStylesheets="{!$User.UIThemeDisplayed == 'Theme4d'}" >
<!--
<apex:page standardController="Applications__c" extensions="CreditMemoController">
    <apex:form >
        <apex:pageBlock title="Loan Information">        
            <apex:pageBlockButtons location="top">
                <apex:commandButton value="Download PDF" action="{!downloadPDF}"/>
            </apex:pageBlockButtons>        
            <apex:pageBlockSection rendered="{!app.Product_Type__c == LOAN_TYPE}" columns="1" showHeader="false" >
                <apex:outputField value="{!app.Loan_Amount__c}"/>
                <apex:outputField value="{!app.Interest_Rate__c}"/>
                <apex:outputField value="{!app.Term__c}"/>
            </apex:pageBlockSection>
            <apex:pageBlockSection rendered="{!app.Product_Type__c == LOC_TYPE}"  columns="1" showHeader="false" >
                <apex:outputField value="{!app.Credit_Limit__c}"/>
                <apex:outputField value="{!app.Interest_Rate__c}"/>
                <apex:outputField value="{!app.Term__c}"/>
                <apex:outputField value="{!app.Draw_Term__c}"/>
            </apex:pageBlockSection>
        </apex:pageblock>
    </apex:form>
    <apex:pageBlock title="Borrower Information">        
        <apex:repeat value="{!app.Parties__r}" var="party" >           
           <apex:pageBlockSection rendered="{!NOT(party.Is_Internal_User__c)}" showHeader="true" columns="1" collapsible="false" title="{! party.clcommon__Party_Type__c}">               
                    <apex:outputText label="Role">{! party.clcommon__Party_Type__c}</apex:outputtext> 
                    <apex:outputField label="Name" value="{!party.clcommon__Contact__c}" 
                            rendered="{!NOT(party.clcommon__Contact__c == null)}"/>
                    <apex:outputField label="Name" value="{!party.clcommon__Account__c}" 
                            rendered="{!NOT(party.clcommon__Account__c == null)}"/>
           </apex:pageBlocksection>         
        </apex:repeat>        
    </apex:pageBlock>
    
    <apex:pageBlock title="Collaterals">
        <apex:repeat value="{!app.Collaterals__r}" var="collateral"> 
            <apex:pageBlockSection columns="1" title="{!collateral.clcommon__Collateral_Name__c}" >

                <apex:pageBlockSection showHeader="false" collapsible="false">
                    <apex:pageBlockSection showHeader="false">
                        <apex:outputField value="{!collateral.clcommon__Collateral_Name__c}"/>
                        <apex:outputText label="Type" value="PROPERTY"/>
                        
                    </apex:pageblockSection>
                </apex:pageBlockSection>
                <apex:iframe src="/apex/pages/CashFlowSummary?propertyId={!collateral.Property__c}" height="380px" frameborder="false"/>
                <apex:pageBlock title="  ">
                    <apex:repeat value="{!propertyToAttachments[collateral.Property__c]}" var="attachment">
                        <apex:outputPanel style="padding-bottom:10px">
                            <apex:image url="{!URLFOR($Action.Attachment.Download, attachment)}"/>                       
                        </apex:outputPanel>
                    </apex:repeat>
                </apex:pageBlock>
            </apex:pageBlockSection>        
        </apex:repeat>
    </apex:pageBlock>
    -->
</apex:page>