<?xml version="1.0" encoding="utf-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>clcommon__No_Recurrence_Day_Mask_if_not_recurring</fullName>
    <active>true</active>
    <description>Recurrence day of week mask cannot be set if the holiday is not marked as Recurring</description>
    <errorConditionFormula>AND(NOT(clcommon__IsRecurrence__c), NOT(ISBLANK(clcommon__RecurrenceDayOfWeekMask__c)))</errorConditionFormula>
    <errorDisplayField>clcommon__RecurrenceDayOfWeekMask__c</errorDisplayField>
    <errorMessage>Recurrence day of week mask cannot be set if the holiday is not marked as Recurring</errorMessage>
</ValidationRule>
