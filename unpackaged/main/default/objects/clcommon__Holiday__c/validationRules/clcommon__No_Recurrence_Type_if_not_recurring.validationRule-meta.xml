<?xml version="1.0" encoding="utf-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>clcommon__No_Recurrence_Type_if_not_recurring</fullName>
    <active>true</active>
    <description>Recurrence Type cannot be selected if the holiday is not marked as Recurring</description>
    <errorConditionFormula>AND(NOT(clcommon__IsRecurrence__c), NOT(ISBLANK(TEXT(clcommon__RecurrenceType__c))))</errorConditionFormula>
    <errorDisplayField>clcommon__RecurrenceType__c</errorDisplayField>
    <errorMessage>Recurrence Type cannot be selected if the holiday is not marked as Recurring</errorMessage>
</ValidationRule>
